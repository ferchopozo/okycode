import { StyleSheet } from 'react-native'
import { BackgroundToolbar } from '../../core/style/globalColor'

export const Styles = StyleSheet.create({
    container: {    
        flex: 1,
        backgroundColor: BackgroundToolbar
    }
})