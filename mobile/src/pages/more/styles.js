import { StyleSheet } from 'react-native'
import { MoreSubTitle } from '../../core/style/globalColor'

export const Styles = StyleSheet.create({
    subTitle :{
        fontSize: 14,
        color: MoreSubTitle
    }
}); 