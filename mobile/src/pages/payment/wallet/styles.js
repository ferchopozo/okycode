import { StyleSheet } from 'react-native'

export const Styles = StyleSheet.create({
    ContainerToolBar: {
       display: "flex",
       flexDirection: "row",
       justifyContent: "flex-end",
       width: '100%'
    }
})