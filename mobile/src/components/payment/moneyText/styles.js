import { StyleSheet } from 'react-native'
import { MoneyTextTitle } from '../../../core/style/globalColor'

export const Styles = StyleSheet.create({
    textStyle : {
    },
    inputStyle: {
        borderWidth: .5,
        borderRadius : 10
    }, 
    title: {
        fontWeight: "bold",
        color: MoneyTextTitle,
        fontSize: 14
    }
})