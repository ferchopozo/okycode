import { StyleSheet } from 'react-native'

export const Styles = StyleSheet.create({
    container: {
        flexDirection: 'column',
        marginLeft: 20,
        marginRight: 20,
        marginBottom: 30
    },
    textTitle: {
        fontSize:20, 
        color:'white',
        fontWeight: '400',
    },
})